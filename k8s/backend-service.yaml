apiVersion: v1
kind: Service
metadata:
  name: carbon-api-service
  namespace: nekazari
  labels:
    app: carbon-api
spec:
  selector:
    app: carbon-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
